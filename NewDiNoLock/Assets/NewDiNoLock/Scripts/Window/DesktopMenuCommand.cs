namespace NewDiNoLock.Window
{
    public enum DesktopMenuCommand
    {
        ToggleVisibility,
        ToggleAutoWalk,
        ToggleAlwaysOnTop,
        ToggleShowAboveFullscreen,
        OpenSettings,
        Exit
    }
}
